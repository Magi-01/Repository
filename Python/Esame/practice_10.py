def count_used_coins(coins, bill):
    used_coins = 0

    for coin in coins:
        if bill >= coin:
            coin

    return used_coins

arr = [1.5,2.5,5.3,10.7,1]
Cost = 10
print(count_used_coins(arr,Cost))